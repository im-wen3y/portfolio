export interface PrintProfileConfig {
	companyLabel: string; // <title>에 쓰이는 지원 회사 또는 프로필 이름
	roleLine: string; // 예: 'Frontend Developer · 7년차'
	portfolioAbout: string;
	featuredProjectIds: string[];
}
