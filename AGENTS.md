# Repository Guidelines

## 적용 범위와 우선순위

이 저장소는 SvelteKit으로 만든 개인 이력서·포트폴리오 사이트다. 채용 담당자가 경력과
기술을 빠르게 파악하도록 정보 전달, 접근성, 성능을 장식 효과보다 우선한다. 이 규칙은
저장소 전체에 적용하며, 하위 디렉터리에 `AGENTS.md`가 생기면 해당 규칙을 우선한다.

- 먼저 `README.md`, 기존 구조와 디자인 패턴을 확인한다.
- 요청 범위 밖의 리팩터링, 콘텐츠 재작성, 디자인 변경은 하지 않는다.
- 기존 사용자 변경을 덮어쓰거나 관련 없는 파일을 포맷하지 않는다.
- 새 라이브러리나 외부 서비스는 임의로 추가하지 않는다.

## 기술 및 Svelte 규칙

실제 `package.json`과 설정 파일을 기준으로 작업한다. 이 프로젝트는 SvelteKit, Svelte 5
runes, strict TypeScript, Vite, pnpm을 사용하며 `$lib`은 `src/lib`을 가리킨다.

- 컴포넌트는 `<script lang="ts">`와 `$props()`, `$state()`, `$derived()` 등 기존 runes
  문법을 사용한다. props와 데이터 구조에는 명확한 타입을 지정한다.
- 파생 가능한 값을 별도 상태로 중복 저장하지 않는다. 전역 상태는 여러 화면에서 실제로
  공유할 때만 사용한다.
- 브라우저 API는 SSR을 고려해 `onMount` 또는 브라우저 여부가 보장된 위치에서 사용한다.
- SvelteKit 라우팅·로딩 규칙을 따르고 내부 링크는 `$app/paths`의 `resolve()`를 사용한다.
- 반복 UI나 로직이 실제로 존재할 때만 컴포넌트·유틸리티로 추출한다.
- `{@html}`은 피한다. 불가피하면 입력이 신뢰 가능한지 확인한다.
- 타입·린트 오류를 `any`, `@ts-ignore`, `@ts-expect-error`, `eslint-disable` 또는 설정 완화로
  숨기지 않는다. `rules/code-style.md`와 `rules/component-style.md`를 따른다.

Prettier 기준은 탭, 작은따옴표, trailing comma 없음, 100자 너비다. Svelte 컴포넌트는
PascalCase, 일반 모듈과 store는 kebab-case, 테스트는 `*.spec.ts` 또는 `*.test.ts`로
명명하고 대상 코드 옆에 둔다.

## 경력 및 콘텐츠 정확성

경력·이력 정보가 필요하면 `~/career-wiki`
([GitHub](https://github.com/im-wen3y/career-wiki))를 우선 확인한다.

- 이름, 회사, 기간, 학력, 프로젝트 성과, 수치, 연락처를 추측하거나 과장하지 않는다.
- 출처에서 확인할 수 없는 사실은 사용자에게 확인하고, 임의의 플레이스홀더를 공개 화면에
  넣지 않는다.
- 기존 의미를 유지하면서 표현만 다듬고, 문제 → 역할 → 기술 → 결과 순서를 선호한다.
- 인쇄 이력서의 담당 범위·문제·조치·결과는 `~했습니다`가 반복되는 서술문보다 짧은 개조식
  문구를 우선한다. 단순히 `~함`으로 치환하지 말고 주체·행동·변화를 보존한 명사형 또는
  결과형으로 끊는다. 자기소개처럼 맥락 전달이 필요한 문단은 자연스러운 서술형을 유지한다.
- 날짜, 기술명, 한국어·영어 표기와 문장 종결을 화면 전체에서 일관되게 유지한다.
- 실제 개인정보를 로그, 테스트 fixture, 예제 데이터에 복제하지 않는다.
- 화면 이력과 인쇄용 이력서·포트폴리오의 핵심 내용이 서로 모순되지 않게 한다.

## 디자인, 반응형 및 접근성

기존 색상, 간격, 타이포그래피, CSS 변수와 컴포넌트 패턴을 재사용한다. 요청 없이 브랜드
색상, 서체, 전체 레이아웃을 바꾸지 않는다. 콘텐츠 길이가 달라도 가로 스크롤이나 카드
깨짐이 없어야 하며 모바일, 태블릿, 데스크톱과 인쇄 레이아웃을 함께 고려한다.

- 시맨틱 HTML을 사용하고 클릭 동작에는 `button` 또는 `a`를 쓴다.
- 모든 기능을 키보드로 사용할 수 있게 하고 포커스 표시를 유지한다.
- 의미 있는 이미지에는 `alt`를, 장식 이미지는 `alt=""` 또는 보조 기술 제외 처리를 한다.
- 아이콘 버튼에는 접근 가능한 이름을 제공하고 제목 계층을 순서대로 유지한다.
- 색상만으로 상태를 전달하지 않으며 충분한 명도 대비를 확보한다.
- 애니메이션은 목적이 분명해야 하며 `prefers-reduced-motion`을 존중한다.
- `!important`, 과도한 `z-index`, 페이지 전용 임시 토큰을 피한다.
- 인쇄 시 여백, 배경, 페이지 분할, 링크와 카드 잘림을 확인한다.

외부 링크와 에셋은 주소·목적·권한을 확인한다. `target="_blank"`에는 필요한 보안 속성을
적용하고, 존재하지 않는 URL이나 배포 도메인을 만들지 않는다. 공개 페이지를 추가하거나
메타데이터를 수정할 때는 고유한 title과 정확한 description을 제공하되 canonical, Open
Graph, SNS 정보는 확인된 값만 사용한다. 외부 폰트, 추적 스크립트, 쿠키를 임의로 추가하지
않는다.

## 검증 및 완료 기준

기본 검증은 정적 검사와 타입 검사까지만 수행한다. 사용자가 정상 동작 또는 런타임 확인을
명시적으로 요청하기 전에는 개발 서버 실행, 브라우저 확인, HTTP 요청, 화면 캡처, 인쇄
미리보기, 테스트 실행 및 프로덕션 빌드를 수행하지 않는다.

“정상 동작 확인”, “직접 테스트”, “브라우저에서 확인” 등 사용자의 명시적인 요청이 있을
때만 런타임 검증을 수행한다. 단순한 구현·수정·검토 요청은 런타임 검증 요청으로 해석하지
않는다.

```bash
pnpm check
```

변경 파일의 포맷이나 린트 확인이 필요하면 해당 파일 범위에서만 정적 검사를 실행한다.
`pnpm test`, `pnpm build`, `pnpm dev`와 브라우저 기반 검증은 사용자가 요청한 경우에만
실행한다.

인증, 라우팅, SSR·CSR 경계, 브라우저 API, 인쇄·PDF처럼 정적 검사만으로 결함 가능성을
충분히 낮추기 어려워 런타임 확인이 반드시 필요하다고 판단되면 임의로 실행하지 않는다.
필요한 확인 항목과 이유를 간단히 설명하고 사용자에게 실행해도 되는지 먼저 묻는다.

런타임 검증을 수행하지 않은 모든 완료 보고에는 다음 문구를 그대로 포함한다.

> 런타임 확인하지 않음

완료 보고에는 변경 내용, 주요 파일, 실행한 검사와 결과, 실행하지 못한 검사와 이유, 남은
위험 또는 사용자 확인 사항을 간단히 적는다.
